import React from 'react';
import { Container, Typography } from '@mui/material';

const EducationalModules = () => {
  return (
    <Container>
      <Typography variant="h4" gutterBottom>Educational Modules</Typography>
      <Typography variant="body1">Interactive quizzes, video tutorials, articles, and infographics on sustainability practices.</Typography>
    </Container>
  );
};

export default EducationalModules;
