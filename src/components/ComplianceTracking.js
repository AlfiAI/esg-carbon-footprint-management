import React from 'react';
import { Container, Typography } from '@mui/material';

const ComplianceTracking = () => {
  return (
    <Container>
      <Typography variant="h4" gutterBottom>Compliance Tracking</Typography>
      <Typography variant="body1">Real-time updates on environmental regulations and standards, with checklists and guides for compliance.</Typography>
    </Container>
  );
};

export default ComplianceTracking;
