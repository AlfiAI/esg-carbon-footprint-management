import React from 'react';
import { Container, Typography } from '@mui/material';

const Infographics = () => {
  return (
    <Container>
      <Typography variant="h5" gutterBottom>Infographics</Typography>
      {/* Placeholder for infographics */}
      <Typography variant="body1">View infographics that explain various sustainability concepts.</Typography>
    </Container>
  );
};

export default Infographics;
